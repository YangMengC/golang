初步解释:
    因为Golang中的sring内部实现由两部分组成，
    一部分是指向字符串起始地址的指针，
    另一部分是字符串的长度，两部分各是8字节，所以一共16字节

结合资料解释：
    sizeof这个函数是根据变量常量的数据类型以及执行代码做使用的计算机配置给出对应结果
    如同上面对于字符串的解释，它在64位的系统上执行得到的便是16
    如果在32位的系统上执行则是8，因为在32位系统上字符串的两个域分别只占据4个字节